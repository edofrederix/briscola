FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      p;
}

boundaryConditions
{
    inlet
    {
        type        Neumann;
        gradients   (0);
    }

    outlet
    {
        type        Dirichlet;
        values      (0);
    }

    wall
    {
        type        Neumann;
        gradients   (0);
    }

    top
    {
        type        Neumann;
        gradients   (0);
    }

    bottom
    {
        type        Neumann;
        gradients   (0);
    }

    empties
    {
        type        empty;
    }
}
