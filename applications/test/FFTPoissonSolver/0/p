FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      p;
}

initialCondition
{

}

boundaryConditions
{
    default
    {
        type    Dirichlet;
        values  (2);
    }

    left
    {
        type    Dirichlet;
        values (0);
    }

    right
    {
        type Dirichlet;
        values (0);
    }

    bottom
    {
        type Dirichlet;
        values (0);
    }

    top
    {
        type Dirichlet;
        values (0);
    }

    front
    {
        type Dirichlet;
        values (0);
    }

    back
    {
        type Dirichlet;
        values (0);
    }
}
