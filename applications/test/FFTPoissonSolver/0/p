FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      p;
}

initialCondition
{

}

boundaryConditions
{
    default
    {
        type    Dirichlet;
        values  (0);
    }

    left
    {
        type    Dirichlet;
        values  (1);
    }

    right
    {
        type    Dirichlet;
        values  (2);
    }

    bottom
    {
        type    Neumann;
        gradients  (3);
    }

    top
    {
        type    Neumann;
        gradients  (4);
    }

    back
    {
        type    Neumann;
        gradients  (5);
    }

    front
    {
        type    Dirichlet;
        values  (6);
    }
}
