FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      p;
}

initialCondition
{

}

boundaryConditions
{
    default
    {
        type    Dirichlet;
        values  (0);
    }

    left
    {
        type    Dirichlet;
        values  (0);
    }

    right
    {
        type    Dirichlet;
        values  (0);
    }

    bottom
    {
        type    Neumann;
        gradients  (0);
    }

    top
    {
        type    Neumann;
        gradients  (0);
    }

    back
    {
        type    Neumann;
        gradients  (0);
    }

    front
    {
        type    Dirichlet;
        values  (0);
    }
}
