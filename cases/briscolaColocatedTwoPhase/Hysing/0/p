FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      p;
}

boundaryConditions
{
    bottomAndTop
    {
        type        Neumann;
        gradients   (0);
    }

    sides
    {
        type        Neumann;
        gradients   (0);
    }
}
