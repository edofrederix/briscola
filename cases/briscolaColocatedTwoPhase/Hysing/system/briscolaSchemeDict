FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      briscolaSchemesDict;
}

ddtSchemes
{
    ddt(U)
    {
        type    Euler;
    }
}

laplacianSchemes
{
    laplacian(v,p)
    {
        type    linearGauss;
    }

    laplacian(p)
    {
        type    linearGauss;
    }

    laplacian(mu,U)
    {
        type    linearGauss;
    }
}

divergenceSchemes
{
    div(phi,U)
    {
        type    midPointGauss;
    }
}

gradientSchemes
{
    grad(p)
    {
        type    midPointGauss;
    }

    grad(p.oldTime)
    {
        type    midPointGauss;
    }

    grad(q)
    {
        type    midPointGauss;
    }

    grad(alpha)
    {
        type    midPointGauss;
    }

    stagGrad(alpha)
    {
        type    midPointGauss;
    }

    grad(U)
    {
        type    midPointGauss;
    }
}

faceGradientSchemes
{
    faceGrad(p)
    {
        type    linear;
    }

    faceGrad(p.oldTime)
    {
        type    linear;
    }

    faceGrad(q)
    {
        type    linear;
    }

    faceGrad(alpha)
    {
        type    linear;
    }
}

faceFluxSchemes
{
    flux(U)
    {
        type    linear;
    }

    flux(grad(alpha))
    {
        type    linear;
    }
}

interpolationSchemes
{
    interp(alpha)
    {
        type    midPoint;
    }

    interp(rho)
    {
        type    midPoint;
    }

    interp(meanRho)
    {
        type    midPoint;
    }

    interp(sigma)
    {
        type    midPoint;
    }

    interp(v)
    {
        type    midPoint;
    }

    interp(min(rho1,rho2))
    {
        type    midPoint;
    }

    interp(kappa)
    {
        type    curvature;
    }
}
