FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      briscolaSchemesDict;
}

ddtSchemes
{
    ddt(U)
    {
        type    Euler;
    }
}

laplacianSchemes
{
    laplacian(rhoInv,p)
    {
        type    linearGauss;
    }

    laplacian(p)
    {
        type    linearGauss;
    }

    laplacian(mu,U)
    {
        type    linearGauss;
    }
}

divergenceSchemes
{
    div(phi,U)
    {
        type    midPointGauss;
    }
}

gradientSchemes
{
    grad(p)
    {
        type    midPointGauss;
    }

    grad(extrapolatedP)
    {
        type    midPointGauss;
    }

    grad(alpha)
    {
        type    midPointGauss;
    }

    stagGrad(alpha)
    {
        type    midPointGauss;
    }

    grad(U)
    {
        type    midPointGauss;
    }
}

faceGradientSchemes
{
    faceGrad(p)
    {
        type    midPoint;
    }

    faceGrad(extrapolatedP)
    {
        type    midPoint;
    }
}

faceFluxSchemes
{
    flux(U)
    {
        type    midPoint;
    }

    flux(grad(alpha))
    {
        type    midPoint;
    }
}

interpolationSchemes
{
    interp(alpha)
    {
        type    midPoint;
    }

    interp(rho)
    {
        type    midPoint;
    }

    interp(rhoInv)
    {
        type    midPoint;
    }

    interp(min(rho1,rho2))
    {
        type    midPoint;
    }

    interp(kappa)
    {
        type    curvature;
    }
}
