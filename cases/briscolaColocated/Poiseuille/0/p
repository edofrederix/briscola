FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      p;
}

boundaryConditions
{
    walls
    {
        type        Neumann;
        gradients   (0);
    }

    inlet_mapped
    {
        type        Neumann;
        gradients   (0);
    }

    inlet
    {
        type        periodic;
    }

    outlet_mapped
    {
        type        Dirichlet;
        values      (0);
    }

    outlet
    {
        type        periodic;
    }

    ib
    {
        type        empty;
    }
}
