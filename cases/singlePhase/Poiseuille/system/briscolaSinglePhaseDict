FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      briscolaSinglePhaseDict;
}

// Solution is u(y) = 0.5/nu*dp/dx*(h^2 - y^2). To have mean(u) = 1 with half
// height h = 1, we thus have dp/dx = 3*nu.

nu          0.01;
source      (0.03 0 0);
