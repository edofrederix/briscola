FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      U;
}

boundaryConditions
{
    walls
    {
        type        Neumann;
        gradients   ((0 0 0));
    }

    empties
    {
        type        empty;
    }
}
