FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      briscolaSchemesDict;
}

gradientSchemes
{
    grad(alpha)
    {
        type    midPointGauss;
    }
}

faceFluxSchemes
{
    flux(U)
    {
        type    midPoint;
    }

    flux(grad(alpha))
    {
        type    midPoint;
    }
}
