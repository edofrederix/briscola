FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      briscolaSchemesDict;
}

ddtSchemes
{
    ddt(T)
    {
        type    Euler;
    }
}

laplacianSchemes
{
    laplacian(lambda,T)
    {
        type    linearGauss;
    }
}
